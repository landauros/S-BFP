services:
  s-bfp:
    build: .
    ports:
      - "5001:5001"
    environment:
      S_BFP_HOST: 0.0.0.0
      S_BFP_OPEN_BROWSER: "0"
      S_BFP_DATA_DIR: /data
    volumes:
      - s-bfp-data:/data

volumes:
  s-bfp-data:
